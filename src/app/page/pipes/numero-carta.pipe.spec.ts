import { NumeroCartaPipe } from './numero-carta.pipe';

describe('NumeroCartaPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroCartaPipe();
    expect(pipe).toBeTruthy();
  });
});
